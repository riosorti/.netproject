@model Capstone.Models.FavouriteModel

@{
    ViewBag.Title = "Index";
}

<h2>Favourite</h2>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.programs)
        </th>*@
    
        <th></th>
    </tr>

    @foreach (var item in Model.programs)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => modelItem.programs.First())
            </td>
           
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.pId }) |*@
                @*@Html.ActionLink("Details", "Details", new { id = item.pId }) *@
               
             
            </td>
        </tr>
    }

</table>

@Html.Raw(TempData["msg"])